import 'package:flutter/material.dart';

class BottomSheetConfig {
  static const double borderRadius = 16.0;
  static const EdgeInsets padding = EdgeInsets.only(top: 40, left: 20, right: 20);
  static const Color barrierColor = Colors.white;
  static const double barrierOpacity = 0.75;
  static const double fieldSpacing = 20.0;
}